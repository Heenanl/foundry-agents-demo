
fastapi>=0.115.0
uvicorn>=0.32.1
pydantic>=2.0.0
python-dotenv>=1.2.1
azure-identity>=1.26.0b1
azure-ai-projects==2.0.0b3
openai>=1.99.0

# Microsoft Agent Framework (preview) — pinned to the build that exposes
# MagenticBuilder, MagenticOrchestratorEvent, GroupChatRequestSentEvent,
# AgentRunUpdateEvent, WorkflowOutputEvent, and AzureAIProjectAgentProvider.
# Newer releases (1.0.0+) renamed/restructured these symbols, so do NOT bump
# without porting magentic_rfp_workflow.py to the new API.
agent-framework-core==1.0.0b260130
agent-framework-azure-ai==1.0.0b260130